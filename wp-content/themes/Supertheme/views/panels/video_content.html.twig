<section class="hero-video" style="background-image: url('{{ panel.video_background.url }}')">
    <div class="row align-middle">
        <div class="column">
            <a href="{{ panel.video_embed }}" class="fancybox-media"></a>
            <div class="content">
                {{ panel.video_content }}
            </div>
        </div>
    </div>
</section>